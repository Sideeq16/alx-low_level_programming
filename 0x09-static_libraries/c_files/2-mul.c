#include "main.h"

/**
* mul - multiply two numbers
* Return: return multiple
* @a: first number
* @b: second number
*/
int mul(int a, int b)
{
	int multiply;

	multiply = a * b;

	return (multiply);
}
