#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - print number of base 10
 *
 * Return: success(0)
 */
int main(void)
{
	char i;
	/* print all number of base 10 */
	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
		putchar('\n');
	return (0);
}
