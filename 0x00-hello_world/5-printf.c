#include <stdio.h>

/**
 * main - Entry  point
 * printf: programing is like building a multigual puzzle
 * Return: success (0)
 */
int main(void)
{
	printf("%s","with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
