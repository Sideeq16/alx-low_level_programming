#include <stdio.h>
#include "calculate.h"

/**
 * add - add to numbers
 * @a: first number
 * @b: second number
 * Return: number negative or positive
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
