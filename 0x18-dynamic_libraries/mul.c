#include <stdio.h>
#include "calculate.h"

/**
 * mul - mul two numbers
 * @a: first number
 * @b: second number
 * Return: number negative or positive
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
