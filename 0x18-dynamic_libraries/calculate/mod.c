#include <stdio.h>
#include "calculate.h"

/**
 * mod - subtract two numbers
 * @a: first number
 * @b: second number
 * Return: number negative or positive
 */
int mod(int a, int b)
{
	int result;

	result = a % b;
	return (result);
}
