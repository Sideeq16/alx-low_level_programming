#include <stdio.h>
#include "calculate.h"

/**
 * div - divide two numbers
 * @a: first number
 * @b: second number
 * Return: number negative or positive
 */
int div(int a, int b)
{
	int result;

	result = a / b;
	return (result);
}
