#include <stdio.h>
#include "calculate.h"

/**
 * sub - subtract two numbers
 * @a: first number
 * @b: second number
 * Return: number negative or positive
 */
int sub(int a, int b)
{
	int result;

	result = a - b;
	return (result);
}
